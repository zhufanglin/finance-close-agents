# 财务月结 AI 系统 — 演示脚本

> 演示账号：**admin / admin123**（管理员）· finance / finance123（财务）· auditor / auditor123（审计）
> 数据期间：2026 年 7 月月结，当前 D+5。演示前建议执行一次 `node prisma/seed.js` 重置数据。

## 一句话开场

「这是一个财务月结 AI 演示系统：**规则做计算、AI 做理解、人做决策**，铁律是——人没点头，账不改。」

## 演示动线（约 10 分钟）

### 1. 指挥中心（首页）— 全局态势

- 6 张统计卡：银行流水 / 账套记录 / 发票 / 凭证 / AI 提案（4 条待审）/ 审计日志
- **5 Agent 集群**：银企对账 / 发票 / 费用报销 / 凭证生成 / 数据质量 Agent，各自的工作状态与产出统计
- 月结时间轴 D-5 → D+6：当前停在 D+5「差异处理与关账准备」

### 2. 银企对账 — 引擎 + AI 闭环（核心亮点）

1. 页面顶部点 **「一键对账」**：
   - 规则引擎按金额+日期+客商三重匹配，几秒内出结果
   - 命中 4 类埋点差异：**一收拆两笔**（联创 89,000 = 50,000+39,000）、**尾差一分**（短信费 28.50 vs 28.49）、**银行有账上无**（招行管理费 200）、**账上有银行无**（在途电费）
   - AI（DeepSeek）对差异逐条解释并**自动生成带分录的提案**，token 成本入审计
2. 任选一行差异点 **「AI 解释」**：展示差异原因分析、处理建议、严重度、本次调用 token 成本

### 3. 发票中心 — 双通道采集

- **图片通道**：上传 `scripts/test_invoice.png` → 本地 RapidOCR 离线识别 → DeepSeek 结构化抽取 → 置信度评分，低于 80% 自动转人工复核
- **数电票通道**：上传 `scripts/test_eticket.xml` → 免 OCR 直解析，字段 100% 准确
- 低置信度发票（62% / 71%）红色警示条 —— AI 不确定时绝不自动过账

### 4. 费用报销 — 规则拦截 + AI 建议

- 右侧提交报销单（事由随意写），提交后自动执行：发票关联核验 → 额度规则 → 税务提示 → **DeepSeek 科目建议**
- 点单号看审核明细弹层：每条规则的通过与拦截原因
- 演示拦截：点 **BX-202607-005（周航 ¥12,800 培训费）「通过」** → 系统拒绝：*超 ¥5,000 需总监加签*（规则优先于 AI）
- 演示过账：通过 **BX-202607-004（孙倩 ¥1,680 招待费）** → 自动生成凭证记-76xx

### 5. 审计中心 — 人工决策 + 全程留痕（灵魂页面）

- Tab1 **AI 提案审批**：
  - 批准「**招商银行账户管理费 200 元未入账**」→ 系统提示 **自动过账生成凭证记-75xx**（借 管理费用-手续费 200 / 贷 银行存款 200）
  - 这就是「AI 建议 → 人拍板 → 账自动生成」的完整闭环
- Tab2 **审计日志**：AI / 人工 / 系统三类筛选，每次 AI 调用的模型、token、成本（元）全部留痕

### 6. 数据质量 — 月结门禁

- 7 项检查：低置信发票 / 未查验发票 / 双侧未匹配流水 / 待审提案 / 待审凭证 / 待审报销
- 数据健康分 + 每项「去处理」直达链接
- AI 调用账本：累计次数与总成本（通常一次完整演示 < ¥0.01）

### 7. 主数据 / 系统设置（快速带过）

- 科目表 / 客商 / 银行账户 —— 所有 AI 建议共用的数据地基
- 集成通道状态：DeepSeek ✓ / RapidOCR ✓ / 数电票解析 ✓ / 银行流水导入 ✓

## 可能被问到的问题

| 问题 | 回答 |
|---|---|
| AI 会不会记错账？ | AI 只出建议（提案），所有写账动作必须人工审批；低置信度自动转人工 |
| AI 挂了怎么办？ | 全链路降级设计：LLM 不可用时自动切换规则引擎，演示不会卡死 |
| 数据安全？ | OCR 本地离线；只有文本摘要进 LLM；每次调用留审计 |
| 成本？ | 单次 AI 调用约 ¥0.0005~0.002，一场演示全部 AI 成本不到 1 分钱 |
| 怎么接真实环境？ | 银行流水导入替换为银企直联、账套侧对接金蝶云星空 OpenAPI 即可，页面与闭环不变 |

## 常用命令

```bash
node prisma/seed.js        # 重置演示数据（含 4 对账差异 + 2 低置信发票 + 5 报销单埋点）
npm run build && npm start # 生产模式启动（dev 模式受本机 safe-delete 限制）
```

测试素材：`scripts/test_invoice.png`（OCR 演示）、`scripts/test_eticket.xml`（数电票）、`scripts/test_bank.csv`（流水导入）。
